import React, { FC } from 'react';

import Container from '@components/Container';

const NotFound: FC = () => {
  return (
    <section>
      <Container>
        <div>NotFound</div>
      </Container>
    </section>
  );
};

export default NotFound;
