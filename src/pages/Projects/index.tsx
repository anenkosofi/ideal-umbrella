import React, { FC } from 'react';

import Container from '@components/Container';

const Projects: FC = () => {
  return (
    <section>
      <Container>
        <div>Projects</div>
      </Container>
    </section>
  );
};

export default Projects;
