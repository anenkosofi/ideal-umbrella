import React, { FC } from 'react';

import Container from '@components/Container';

const Cart: FC = () => {
  return (
    <section>
      <Container>
        <div>Cart</div>
      </Container>
    </section>
  );
};

export default Cart;
