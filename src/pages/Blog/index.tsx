import React, { FC } from 'react';

import Container from '@components/Container';

const Blog: FC = () => {
  return (
    <section>
      <Container>
        <div>Blog</div>
      </Container>
    </section>
  );
};

export default Blog;
