import React, { FC } from 'react';

import Container from '@components/Container';

const About: FC = () => {
  return (
    <section>
      <Container>
        <div>About</div>
      </Container>
    </section>
  );
};

export default About;
