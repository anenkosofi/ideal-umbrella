import React, { FC } from 'react';

import Container from '@components/Container';

const Home: FC = () => {
  return (
    <section>
      <Container>
        <div>Home</div>
      </Container>
    </section>
  );
};

export default Home;
