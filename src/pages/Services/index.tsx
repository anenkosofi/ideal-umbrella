import React, { FC } from 'react';

import Container from '@components/Container';

const Services: FC = () => {
  return (
    <section>
      <Container>
        <div>Services</div>
      </Container>
    </section>
  );
};

export default Services;
