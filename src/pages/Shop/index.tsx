import React, { FC } from 'react';

import Container from '@components/Container';

const Shop: FC = () => {
  return (
    <section>
      <Container>
        <div>Shop</div>
      </Container>
    </section>
  );
};

export default Shop;
